'use client'; import { useRouter } from 'next/navigation'; import { createClient } from '@/lib/supabase/browser'; export function DeleteProduct({id}:{id:string}){const r=useRouter();async function del(){if(!confirm('Delete this product?'))return;const {error}=await createClient().from('products').delete().eq('id',id);if(error)alert(error.message);else{r.push('/admin/products');r.refresh();}}return <button className="danger" onClick={del}>Delete product</button>}
