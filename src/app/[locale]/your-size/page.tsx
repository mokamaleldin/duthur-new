import { SizeFinder } from '@/components/SizeFinder'; import { isLocale } from '@/lib/i18n'; import { notFound } from 'next/navigation';
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="section page"><p className="eyebrow">DUTHUR SIZING</p><h1 className="page-title">Find Your Size</h1><SizeFinder locale={locale}/></main>}
