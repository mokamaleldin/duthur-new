import { createClient } from '@/lib/supabase/server'; export default async function Page(){const s=await createClient();const {data}=await s.from('contact_messages').select('*').order('created_at',{ascending:false});return <><div className="admin-title"><div><p>Inbox</p><h1>Messages</h1></div></div><section className="admin-card messages">{(data||[]).map(m=><article key={m.id}><div><strong>{m.name}</strong><span>{m.email}</span><time>{new Date(m.created_at).toLocaleString()}</time></div><p>{m.message}</p></article>)}</section></>}
