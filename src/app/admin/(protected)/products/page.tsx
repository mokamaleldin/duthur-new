import Link from 'next/link'; import { createClient } from '@/lib/supabase/server';
export default async function Products(){const s=await createClient();const {data}=await s.from('products').select('*,product_variants(stock_quantity)').order('created_at',{ascending:false});return <><div className="admin-title"><div><p>Catalog</p><h1>Products</h1></div><Link className="primary inline" href="/admin/products/new">+ Add product</Link></div><section className="admin-card"><div className="admin-table head"><span>Product</span><span>Price</span><span>Stock</span><span>Status</span></div>{(data||[]).map(p=><Link className="admin-table table-row" key={p.id} href={`/admin/products/${p.id}`}><span>{p.title_en}</span><span>{Number(p.base_price).toFixed(2)} TL</span><span>{(p.product_variants||[]).reduce((a:any,v:any)=>a+v.stock_quantity,0)}</span><span className={`status ${p.active?'delivered':'cancelled'}`}>{p.active?'active':'draft'}</span></Link>)}</section></>}
