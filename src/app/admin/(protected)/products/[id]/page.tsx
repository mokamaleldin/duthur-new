import { notFound } from 'next/navigation'; import { createClient } from '@/lib/supabase/server'; import { ProductForm } from '@/components/admin/ProductForm'; import { DeleteProduct } from '@/components/admin/DeleteProduct';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data}=await s.from('products').select('*,product_variants(*),product_images(*)').eq('id',id).single();if(!data)notFound();return <><div className="admin-title"><div><p>Catalog</p><h1>Edit product</h1></div><DeleteProduct id={id}/></div><ProductForm initial={data as any}/></>}
