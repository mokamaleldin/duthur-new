import { createClient } from '@/lib/supabase/server'; import { DiscountManager } from '@/components/admin/DiscountManager'; export default async function Page(){const s=await createClient();const {data}=await s.from('discount_codes').select('*').order('created_at',{ascending:false});return <><div className="admin-title"><div><p>Sales</p><h1>Discounts</h1></div></div><DiscountManager discounts={data||[]}/></>}
